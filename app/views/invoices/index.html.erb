<%= render 'subbar', invoices: @invoices %>
<%= render(StageComponent.new()) do %>
  <%= render(NullComponent.new(title: "No Invoice Selected")) do %>
    <%= render(IconComponent.new(name: "file-text-o")) %>
  <% end %>
<% end %>
<!--<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Number</th>
      <th>Date</th>
      <th>Due</th>
      <th>Notes</th>
      <th>Client</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invoices.each do |invoice| %>
      <tr>
        <td><%= invoice.title %></td>
        <td><%= invoice.number %></td>
        <td><%= invoice.date %></td>
        <td><%= invoice.due %></td>
        <td><%= invoice.notes %></td>
        <td><%= @clients.find(invoice.client_id).name %></td>
        <td><%= link_to 'Show', invoice %></td>
        <td><%= link_to 'Edit', edit_invoice_path(invoice) %></td>
        <td><%= link_to 'Destroy', invoice, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %> 
  </tbody>
</table>-->
