<%= item.title %>
<%= item.description %>
<%= item.price %>
<%= item.qty %>
<%= link_to invoice_item_path(@invoice, item.id), method: :delete, data: { confirm: "Are you sure?" }, class: "vault-link__action" do %>Delete<% end %>
